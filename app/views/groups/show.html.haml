.page-header
  %h3 Projects
  - @group.projects.in_office(@office).each do |project|
    %a{href: "#{project_url(project)}"}
      = project.name
    %br

.page-header
  %h4 Skills
  .well
    %h4{:style => "color: #515151;"} Tap skills for details
    #skillcloud{:style => "height: 400px;"}

:javascript
  $(function() {
    var skill_array = JSON.parse('#{@skill_array.to_json}');
    $("#skillcloud").jQCloud(skill_array);
  });

-# old page content
-#.page-header
  -#%h3 Group information
-#.row
  -#.col-sm-12
    -#%p#notice= notice
    -#%h4 Group #{@group.name} Members:
    -#%table.table
      -#%thead
        -#%tr
          -#%th Name
          -#%th Email
          -#%th Description
          -#%th Tags
          -#%th{:colspan => "3"}
      -#%tbody
        -#- @group.people.each do |person|
          -#%tr
            -#%td= person.name
            -#%td= person.email
            -#%td= person.description
            -#%td= person.tag_list

    -#= link_to 'Edit', edit_group_path(@group)
    -#|
-#    \#{link_to 'Back', groups_path}
